{
  "name": "resolve-vector-omp",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-model reasoning, review, and synthesis for Oh My Pi.",
  "type": "module",
  "omp": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@oh-my-pi/pi-coding-agent": "^17.0.7",
    "typescript": "^5.9.2"
  }
}
